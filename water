#include <cs50.h>
#include <stdio.h>

int main (void)
{
    int minutes; 
    int bottles;
    do {
        printf("minutes: ");
        minutes = get_int();
    }
    while (minutes < 1);
    bottles = minutes*12;

    printf("bottles: %i\n", bottles);
}
